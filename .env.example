# X API credentials (for the account that owns the campaign posts)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
X_BEARER_TOKEN=

# Account Activity API
WEBHOOK_ENV=              # The dev environment name set up in the X developer portal
WEBHOOK_URL=              # Your publicly accessible HTTPS URL (e.g., https://your-domain.com/webhook/twitter)

# App config
PORT=3000
